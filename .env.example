# Copy this file to .env and fill in real values. .env is git-ignored.

# Required only for a live run of scripts/run_slice.py (omit --fake).
# Without this set, run_slice.py automatically falls back to FakeProvider.
ANTHROPIC_API_KEY=

# Optional overrides:
# ANTHROPIC_MODEL=claude-sonnet-5
# DB_PATH=./bid_intel.db
